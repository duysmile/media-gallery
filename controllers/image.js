const { imageRepository } = require('../repositories');

class ImageController {
    async uploadImage(req, res, next) {
        try {
            
        } catch (error) {
            next(error);
        }
    }
}

module.exports = new ImageController();
